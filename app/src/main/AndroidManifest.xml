<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Batch §5: wajib sejak targetSdk 34 (Android 14) untuk setForeground()
         bertipe dataSync di AutoSortWorker, lihat AutoSortNotification.kt -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- [Fitur baru 2026-08-19, in-app updater] INTERNET wajib utk cek
         GitHub Releases API + download APK. REQUEST_INSTALL_PACKAGES wajib
         sejak Android 8 utk memicu ACTION_VIEW instalasi APK dari luar Play
         Store (FileProvider content:// URI yang sudah ada, lihat provider
         di bawah); tanpa ini dialog instal ditolak sistem. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".PromptVaultApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PromptVault">

        <!-- [Fix layout/inset 2026-08-27] windowSoftInputMode dipasangkan dgn
        Modifier.imePadding() baru di NavHost (MainActivity.kt) supaya
        keyboard mendorong konten (bukan menimpa) secara konsisten API 26-34;
        adjustResize adalah kombinasi resmi yg direkomendasikan bareng
        imePadding() utk migrasi edge-to-edge. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".worker.BootCompletedReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- [Fase 3.1 roadmap, 2026-08-21] Widget home screen "Scan Sekarang".
             exported=true WAJIB utk widget (system launcher proses terpisah
             yang memicu broadcast APPWIDGET_UPDATE) — ini pola standar
             Android, BUKAN celah keamanan baru (custom action ACTION_SCAN_NOW
             cuma trigger scan Downloads yang sudah bisa dipicu app itu
             sendiri kapan saja lewat tombol "Scan Sekarang" di Home, tidak
             expose data/permission baru). Lihat javadoc ScanWidgetProvider.kt. -->
        <receiver
            android:name=".widget.ScanWidgetProvider"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.elprompter.promptvault.widget.ACTION_SCAN_NOW" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_scan_info" />
        </receiver>

        <!-- Batch §5: WorkManager sudah mendeklarasikan service ini lewat manifest
             merge library-nya sendiri, tapi di-override eksplisit di sini supaya
             foregroundServiceType="dataSync" PASTI ter-set (bukan bergantung ke
             default library yang bisa berubah antar versi), pola resmi yang
             didokumentasikan Android untuk WorkManager + foreground service type. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            tools:node="merge" />

        <!-- [Fitur baru 2026-08-17, integrasi Shizuku] Provider ini WAJIB
             dideklarasikan manual (authorities pakai applicationId, tidak
             bisa datang dari manifest library begitu saja) supaya app bisa
             menerima binder Shizuku, baik lewat Shizuku Manager (root/
             wireless debugging) maupun `adb shell` langsung. Lihat
             shizuku/ShizukuManager.kt untuk sisi konsumsinya. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
