<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- v3.0.0: parent diganti dari Material.Light -> Material (non-light).
         Dark mode adalah satu-satunya mode aplikasi; tidak boleh ada jalur
         sistem (dialog bawaan, dsb) yang diam-diam jatuh ke chrome terang. -->
    <style name="Theme.PromptVault" parent="android:Theme.Material.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/pv_amoled_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.PromptVault</item>
    </style>
</resources>
